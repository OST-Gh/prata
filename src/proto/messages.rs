///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
pub struct Connect;
pub struct Disconnect;
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
impl Default for Connect {
	#[inline(always)]
	fn default() -> Self { Self }
}

impl Default for Disconnect {
	#[inline(always)]
	fn default() -> Self { Self }
}
